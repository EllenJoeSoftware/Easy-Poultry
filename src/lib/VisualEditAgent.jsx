/**
 * VisualEditAgent was a Base44-platform iframe-editing tool.
 * Stubbed for the standalone build — we don't need it outside the Base44 host.
 */
export default function VisualEditAgent() {
  return null;
}
